# Local Sample Outputs

Phase 2 generates representative JSON files here for all four document types. The JSON files are ignored by Git because they preserve source text from the supplied dataset.

Before the repository is made public, create explicitly redacted or synthetic examples for any output that will be committed or included in the presentation.
